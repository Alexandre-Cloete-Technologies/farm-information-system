# Supabase project the app reads from.
# Both values are safe to expose to the browser: the publishable key can only do
# what Row Level Security allows an authenticated user to do.
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxx
